# Fiber Closure Detection Service Configuration
# Copy to .env and modify as needed

# Model paths
FIBER_MODEL_CFG=cfg/fiber.cfg
FIBER_MODEL_WEIGHTS=weights/fiber_best.weights
FIBER_MODEL_DATA=cfg/fiber.data

# Detection thresholds
FIBER_CONFIDENCE_THRESHOLD=0.5
FIBER_NMS_THRESHOLD=0.45

# Server settings
FIBER_HOST=127.0.0.1
FIBER_PORT=8000
